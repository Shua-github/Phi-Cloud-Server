{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: FastAPI (with dev)",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/phi_cloud_server",  // 启动路径
      "args": [],
      "jinja": true,
      "justMyCode": true,
      "env": {
        "DEV": "true" // 启用dev
      }
    }
  ]
}
